import React from 'react';

const Fallback = () => {
  return (
    <div className='react-fallback'>
      <div className='banner'>Please wait while the content is loading...</div>
    </div>
  );
};

export default Fallback;
